# live-search using bootstrap , ajax and php this is a simple live search input page.
Type in a name and it retrieves all the data from the database without having to refresh the whole page.
Run the database file using xampp.
use the database named searchperson.sql
